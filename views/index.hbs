<form action="/test/submit" method="post">
    <input type="text" name="id" />
    <button type="submit">SUBMIT</button>
</form>
<hr/>
<h1>{{ title }}</h1>
<p>Hey, welcome to {{ title }}!</p>
{{# if condition }}
    Condition is met
{{ else }}
    Condition not met
{{/if}}

{{# each anyArray}}
    {{ this }}
{{/each}}

<hr/>

{{# each anyArray as |val key|}}
    {{ val }} , {{ key }} oops
{{/each}}